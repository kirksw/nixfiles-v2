Use the `subagent` tool in chain mode with `agentScope: "user"` and this flow:

1. `10xBEAST`: do fast reconnaissance and identify the real constraints and blockers.
2. `the-architect`: produce a concrete, phased implementation plan.

Task:

{{args}}
